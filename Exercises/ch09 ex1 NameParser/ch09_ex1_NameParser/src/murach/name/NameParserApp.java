//Emma Ko 4.9.20
//Java wouldn't let me run this for some reason, so I couldn't test it. It just keeps running the previous code I did.
package murach.name;

import java.util.Scanner;

public class NameParserApp {

    public static void Main(String [] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter a name: ");
        String name = sc.nextLine();
        System.out.println();
        sc.close();
    String Name = name + "";
    String Name += "";
    }
}
